import { PullRefreshDirective } from './pull-refresh.directive';

describe('PullRefreshDirective', () => {
  it('should create an instance', () => {
    const directive = new PullRefreshDirective();
    expect(directive).toBeTruthy();
  });
});
